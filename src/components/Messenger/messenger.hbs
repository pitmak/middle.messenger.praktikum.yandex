<div class="{{ styles.messenger }}">
  {{#if selectedChat }}
    <div class="{{ styles.messenger-messages }}">
      {{#each messages}}
        {{{this}}}
      {{/each}}
    </div>
    <div class="{{ styles.messenger-footer }}">
      {{{input}}}
      {{{button}}}
    </div>
  {{else}}
    Выберите чат...
  {{/if}}
</div>
